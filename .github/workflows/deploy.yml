name: Deploy to PlanetHoster

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Allows manual triggering from any branch
    inputs:
      test_mode:
        description: 'Test mode (only test connection, no deployment)'
        required: false
        default: 'false'
        type: boolean

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Test FTP Connection
      run: |
        echo "🔍 Testing FTP connection first..."
        sudo apt-get update
        sudo apt-get install -y lftp
        
        echo "Testing connection to: ${{ secrets.FTP_SERVER }}"
        echo "Username: ${{ secrets.FTP_USERNAME }}"
        echo "Password: [HIDDEN]"
        
        # Create lftp script file to avoid shell escaping issues
        echo "set ftp:ssl-allow no" > /tmp/ftp_test.txt
        echo "set ftp:passive-mode on" >> /tmp/ftp_test.txt
        echo "set net:timeout 30" >> /tmp/ftp_test.txt
        echo "set net:max-retries 3" >> /tmp/ftp_test.txt
        echo "set ftp:list-options -a" >> /tmp/ftp_test.txt
        echo "open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_SERVER }}" >> /tmp/ftp_test.txt
        echo "pwd" >> /tmp/ftp_test.txt
        echo "ls -la | head -10" >> /tmp/ftp_test.txt
        echo "cd public_html" >> /tmp/ftp_test.txt
        echo "pwd" >> /tmp/ftp_test.txt
        echo "ls -la | head -10" >> /tmp/ftp_test.txt
        echo "quit" >> /tmp/ftp_test.txt
        
        echo "Running FTP connection test..."
        if lftp -f /tmp/ftp_test.txt 2>&1; then
          echo "✅ FTP connection successful!"
        else
          echo "❌ FTP connection failed!"
          echo ""
          echo "Troubleshooting steps:"
          echo "1. Verify FTP_SERVER secret: ${{ secrets.FTP_SERVER }}"
          echo "2. Verify FTP_USERNAME secret: ${{ secrets.FTP_USERNAME }}"
          echo "3. Check FTP_PASSWORD secret (special characters like ! should work)"
          echo "4. Try connecting manually with an FTP client to verify credentials"
          exit 1
        }
        
        echo "✅ FTP connection successful!"
        
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: projects/javascript/package-lock.json
        
    - name: Install dependencies
      working-directory: projects/javascript
      run: npm ci
      
    - name: Build React app
      working-directory: projects/javascript
      run: CI=false npm run build
      
    - name: Prepare deployment files
      working-directory: projects/javascript
      run: |
        echo "📦 Preparing deployment files..."
        
        # Create deployment directory
        mkdir -p deployment/public_html
        
        # Step 1: Copy React build files to public_html root
        echo "📁 Copying React build files..."
        cp -r build/* deployment/public_html/
        
        # Step 2: Copy PHP files to public_html root
        echo "📁 Copying PHP files..."
        cp server/*.php deployment/public_html/ 2>/dev/null || echo "No PHP files found in server/"
        
        # Step 3: Copy .htaccess for React Router
        echo "📁 Copying .htaccess..."
        if [ -f .htaccess ]; then
          cp .htaccess deployment/public_html/
        else
          echo "⚠️  .htaccess not found - React Router may not work correctly"
        fi
        
        # Verify critical files exist
        echo "✅ Verifying deployment structure..."
        echo "Files in deployment/public_html/:"
        ls -la deployment/public_html/ | head -20
        echo ""
        echo "Checking for critical files:"
        [ -f deployment/public_html/index.html ] && echo "✅ index.html found" || echo "❌ index.html missing"
        [ -d deployment/public_html/assets ] && echo "✅ assets/ folder found" || echo "❌ assets/ folder missing"
        [ -d deployment/public_html/static ] && echo "✅ static/ folder found" || echo "❌ static/ folder missing"
        [ -f deployment/public_html/.htaccess ] && echo "✅ .htaccess found" || echo "⚠️  .htaccess missing"
        
    - name: Deploy to PlanetHoster via LFTP
      run: |
        # Install lftp
        sudo apt-get update
        sudo apt-get install -y lftp
        
        # Debug: Show what we're about to upload
        echo "=== FILES TO UPLOAD ==="
        ls -la projects/javascript/deployment/public_html/
        echo "=== INDEX.HTML CONTENT (first 10 lines) ==="
        head -10 projects/javascript/deployment/public_html/index.html
        
        # Create lftp script for deployment
        echo "set ftp:ssl-allow no" > /tmp/ftp_deploy.txt
        echo "set ftp:passive-mode on" >> /tmp/ftp_deploy.txt
        echo "set net:timeout 30" >> /tmp/ftp_deploy.txt
        echo "set net:max-retries 3" >> /tmp/ftp_deploy.txt
        echo "open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_SERVER }}" >> /tmp/ftp_deploy.txt
        echo "cd public_html" >> /tmp/ftp_deploy.txt
        echo "lcd projects/javascript/deployment/public_html" >> /tmp/ftp_deploy.txt
        echo "mirror -R --verbose --exclude-glob .git* --exclude-glob node_modules* --exclude-glob .env*" >> /tmp/ftp_deploy.txt
        echo "quit" >> /tmp/ftp_deploy.txt
        
        # Run deployment
        lftp -f /tmp/ftp_deploy.txt
        
        # Debug: Verify upload by listing remote directory
        echo "=== VERIFYING UPLOAD ==="
        echo "set ftp:ssl-allow no" > /tmp/ftp_verify.txt
        echo "open -u ${{ secrets.FTP_USERNAME }},${{ secrets.FTP_PASSWORD }} ${{ secrets.FTP_SERVER }}" >> /tmp/ftp_verify.txt
        echo "cd public_html" >> /tmp/ftp_verify.txt
        echo "ls -la" >> /tmp/ftp_verify.txt
        echo "quit" >> /tmp/ftp_verify.txt
        lftp -f /tmp/ftp_verify.txt
